# PolyArc.AI — Safari Web Extension port

A faithful Safari port of the [PolyArc.AI Chrome extension](https://github.com/hcilabsdev/polyarc).
(The user-facing name is **PolyArc.AI** — distinct from the unrelated game studio Polyarc.
The `polyarc.ai` backend domain and `ai.polyarc.safari` bundle id are unchanged.)
Same honest decision-support card on Polymarket pages — facts, not advice.

## What changed from the Chrome version

**Nothing in the extension code.** `content.js`, `popup.html`, `popup.js`, `card.css`,
`manifest.json` and the icons are copied **verbatim** from the upstream `extension/`
folder. PolyArc uses only Web Extension APIs that Safari supports:

| API used | Safari support |
|---|---|
| `chrome.storage.sync` (`get`/`set`, callback form) | ✅ |
| `action.default_popup` / `default_icon` | ✅ |
| `host_permissions`, `content_scripts`, `permissions: ["storage"]` | ✅ |
| `crypto.randomUUID`, `fetch` (cross-origin via host_permissions) | ✅ |
| Manifest V3, **no background service worker** | ✅ (simplest case) |

Safari exposes the `chrome.*` namespace to web extensions, so the source is drop-in.
The only Safari-specific part is the **macOS app wrapper** that Safari requires to host
an extension — generated by Apple's `safari-web-extension-converter`, below.

## Prerequisites

| Need | This machine | Action |
|---|---|---|
| macOS 26 / arm64 | ✅ | — |
| Swift 6.3, Node 26 | ✅ | — |
| **Full Xcode** | ❌ (only Command Line Tools) | Install from the App Store, then `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` |
| **Code-signing identity** | ❌ (0 found) | Add your Apple ID in Xcode → Settings → Accounts (free Personal Team for local; paid $99/yr only to publish) |

## Build & run

`convert.sh` generates **both macOS and iOS/iPadOS** app targets from the one shared
extension (Safari on iOS/iPadOS supports Web Extensions since iOS 15).

```bash
./convert.sh        # generate PolyArc-Xcode/ from extension/  (needs full Xcode)
./build.sh          # build the macOS app
./build.sh ios      # build the iOS app for the iOS Simulator (no signing needed)
```

### macOS
1. Run the built **PolyArc.AI.app** once (under `build/Build/Products/Debug/`) — launching
   it registers the extension with Safari.
2. **Safari → Settings → Extensions → PolyArc.AI** → enable it.
3. For an unsigned/dev build: **Safari → Settings → Advanced → "Show features for web
   developers"**, then **Develop → Allow Unsigned Extensions** (resets each Safari launch).
4. Open any market on `polymarket.com` — the card appears bottom-right, same as Chrome.

### iOS / iPadOS
1. `./build.sh ios` builds for the Simulator (no signing). Install the resulting
   `.app` in a booted Simulator, or just open the project in Xcode, pick the
   **PolyArc.AI (iOS)** scheme + a Simulator, and Run.
2. In the Simulator: **Settings → Apps → Safari → Extensions → PolyArc.AI** → enable, and
   grant access to `polymarket.com`.
3. Open `polymarket.com` in mobile Safari — same card.
4. **Physical device:** requires signing with your Apple ID Team — open the project in
   Xcode, select your Team under Signing & Capabilities, and Run to the device.

> Prefer the GUI? Skip `build.sh` and open `PolyArc-Xcode/PolyArc.AI/PolyArc.AI.xcodeproj`
> in Xcode, pick the **PolyArc.AI (macOS)** scheme, set your Team under Signing &
> Capabilities, and press Run.

## Backend / privacy

Identical to upstream: the card is fetched from the PolyArc.AI backend (`https://polyarc.ai`
by default; override in the popup). The only data sent is the anonymized `logEvent`
payload defined in `content.js`. No identity, no wallet, no bets. See upstream
`PRIVACY.md`.

## Layout

```
extension/          web-extension resources (verbatim from upstream) — what Safari runs
convert.sh          extension/  ->  Safari Xcode project (macOS + iOS targets)
build.sh            xcodebuild the wrapper app  (./build.sh [macos|ios])
PolyArc-Xcode/      generated by convert.sh (git-ignored)
```

## Distribution

Safari extensions ship **only** via the Mac App Store (there is no "load unpacked zip"
for end users). That requires a paid Apple Developer account, an App Store Connect
record, and notarization — Xcode's Product → Archive → Distribute App flow handles it.
